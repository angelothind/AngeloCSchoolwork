# repeat until array is size 0
# calc mid point of current array
# If target is at middle
# otherwise, if target less than middle change end point to be to the left, otherwise if target bigger than mid cahnge start point

snum_list = [0,3,8,15,17,28,38,51,54,55,59]
start_point = 0
end_point = len(snum_list) - 1
num_of_items = range(len(snum_list[start_point,end_point]))
num_found = False
Runout = False